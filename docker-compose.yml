services:
  trader:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PAPER_TRADING: "true"
      LIVE_TRADING_ENABLED: "false"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
